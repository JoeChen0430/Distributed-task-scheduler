[pytest]
# auto: async test functions and async fixtures run without a per-test marker.
# The pure tests (test_graph.py, test_retry.py) are sync and unaffected; the
# integration tests (test_integration.py) are async and need this.
asyncio_mode = auto
